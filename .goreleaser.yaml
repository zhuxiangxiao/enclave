version: 2

project_name: enclave

before:
  hooks:
    - go mod tidy

builds:
  - id: enclave
    main: ./cmd/enclave
    binary: enclave
    env:
      - CGO_ENABLED=0
    # enclave is a macOS-only tool.
    targets:
      - darwin_amd64
      - darwin_arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/zhuxiangxiao/enclave/v3/internal/version.Version={{.Version}}
      - -X github.com/zhuxiangxiao/enclave/v3/internal/version.CommitHash={{.ShortCommit}}

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE

checksum:
  name_template: checksums.txt

changelog:
  disable: true

brews:
  - name: enclave
    repository:
      owner: zhuxiangxiao
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/zhuxiangxiao/enclave"
    description: "A tool to run any command in a sandboxed environment using macOS's sandbox-exec"
    license: "MIT"
    install: |
      bin.install "enclave"

release:
  github:
    owner: zhuxiangxiao
    name: enclave
  draft: false
  # Automatically marks as prerelease if the tag contains a hyphen (e.g. v3.0.0-rc1).
  prerelease: auto
  # Overwrites the existing release if re-run for the same tag.
  mode: replace
